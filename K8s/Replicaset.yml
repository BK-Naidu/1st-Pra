apiVersion: apps/v1
kind: ReplicaSet
meatadata:
  name: nginx
spec:
  replicas: 1
  selector:
  template: # Pod Template
    metadata:
    spec:
      containers:
      - name: nginx-container
      imge: nginx
      ports:
      - containerPort: 80

